<span class="flex gap-2 bg-slate-700 text-slate-300 text-base rounded-lg px-2 py-1 mr-2 w-1/6 justify-center">
  <%= button_to '+', add_to_cart_path(sneaker), method: :post, class: 'font-bold' %>
  <span><%= @cart.sneaker_count(sneaker) %></span>
  <%= button_to '-', remove_from_cart_path(sneaker), method: :delete, class: 'font-bold'%>
</span>

<!--absolute right-2 top-2-->